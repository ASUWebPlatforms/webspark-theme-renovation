{#
/**
 * @file
 * Template for a block.
 */
#}
{% set classes = [
  'row',
  'block',
  'block-' ~ configuration.provider|clean_class,
  'block-' ~ plugin_id|clean_class,
] %}
<div{{ attributes.addClass(classes) }}>
  {{ title_prefix }}
  {{ title_suffix }}

  {% block content %}
    {{ content|without('field_anchor') }}
  {% endblock %}

</div>
